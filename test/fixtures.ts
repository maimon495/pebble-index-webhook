// Throwaway test values — never real credentials.
export const TEST_OAUTH_CREDENTIALS = {
  client_id: "test-client-id.apps.googleusercontent.com",
  client_secret: "test-client-secret",
  refresh_token: "test-refresh-token",
};
